# ── DiTing Environment Variables ──
# Copy this file to .env and modify as needed.
# Used by: docker-compose.yml (env_file) or pydantic-settings auto-load.

# ★ ASR Worker endpoints (REQUIRED for Docker — point to your GPU server)
# Format: JSON dict mapping engine name to URL
ASR_WORKERS={"sensevoice":"http://gpu-server:8001","whisper":"http://gpu-server:8002","qwen3asr":"http://gpu-server:8003"}

# Default ASR engine
# ASR_ENGINE=sensevoice

# System Configuration
# TZ=Asia/Shanghai

# Data directories (defaults are usually fine)
# DATA_DIR=data
# DB_PATH=data/db/diting_prod.db
# TEMP_DOWNLOADS_DIR=data/temp_downloads
# TEMP_UPLOADS_DIR=data/temp_uploads
# COVERS_DIR=data/covers
# MEDIA_CACHE_DIR=data/media_cache
