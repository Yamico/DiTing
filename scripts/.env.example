# ── DiTing PC Desktop Deployment ──
# Copy this file to .env and modify as needed.
# Used by: scripts/run_tray.py → app/server.py (via pydantic-settings)

# ASR Worker endpoints (auto-managed by run_tray.py for local workers)
# ASR_WORKERS={"sensevoice":"http://localhost:8001","whisper":"http://localhost:8002","qwen3asr":"http://localhost:8003"}

# Default ASR engine
# ASR_ENGINE=sensevoice

# System Configuration
# TZ=Asia/Shanghai

# Data directories (defaults are usually fine)
# DATA_DIR=data
# DB_PATH=data/db/diting_prod.db
# TEMP_DOWNLOADS_DIR=data/temp_downloads
# TEMP_UPLOADS_DIR=data/temp_uploads
# COVERS_DIR=data/covers
# MEDIA_CACHE_DIR=data/media_cache
